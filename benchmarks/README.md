# Benchmarks

Benchmark contracts are defined in [`docs/04-testing-and-benchmark-spec.md`](../docs/04-testing-and-benchmark-spec.md) and Stage 17.

Stage 21 creates this entry point only. No benchmark result is claimed until an actual agent execution is recorded. Stage 22 adds the first deterministic and behavioural fixtures.
