# diagnose-constraint

Validate the symptom and metric first. Generate competing causal layers. Use evidence to narrow them. Name the constraint only when supported; otherwise return a provisional diagnosis and the cheapest distinguishing evidence.
