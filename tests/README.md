# Tests

Stage 22 will add deterministic repository/economics tests for the first vertical. Stage 24 adds clean external installation smoke tests.

Do not mark planned semantic tests as passing without executing an agent against the benchmark fixture.
